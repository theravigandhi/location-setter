// Fill this in with your real Firebase project's Web app config
// (Firebase Console -> Project settings -> General -> Your apps -> Web app -> SDK setup and
// configuration -> Config). This file is safe to commit: Firebase Web config values are not
// secrets - the real protection is firestore.rules plus this admin account being the only
// non-anonymous Firebase Auth user in the project.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
